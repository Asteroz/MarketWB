@using MarketWB.Web.ViewModels.Cabinet
@{
    Layout = "_Layout";
}
@model NewsViewModel


    <!-- start: Content -->
    <div id="content">

        <div class="panel-body">
            <div class="col-md-12">
                <h3 class="animated fadeInLeft">Новости</h3>
                  @foreach (var tag in Model.Tags)
                  {
                     <a asp-action="News" asp-controller="Cabinet" asp-route-tag="@tag"><button class="btn btn-primary">@tag</button></a>
                  }
            </div>
        </div>

        <div class="col-md-12 top-20 padding-0">
            @foreach (var post in Model.Posts)
            {
                <div class="col-md-12">
                    <div class="panel">
                        <div class="panel-heading">
                            <h3>
                                @post.Caption
                                <small>@post.CreatedAt</small>
                                @foreach (var tag in post.TagsList)
                                {
                                    <span class="badge badge-primary">@tag</span>
                                }
                            </h3>
                        </div>
                        <div class="panel-body">
                            <div class="col-md-12 list-timeline">
                                <div class="col-md-12 list-timeline-section bg-light">
                                    <div class="col-md-5 col-sm-7">
                                        <img height="300px" width="400px" src="@post.ImgURL" />
                                    </div>
                                    <div class="col-md-7 col-sm-5">
                                        <p class="">
                                            @post.Description
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                 </div>           
             }
        </div>
    </div>

